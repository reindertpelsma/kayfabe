# R30 fb-cpu-view — NEGATIVE CONTROL (guest-side view on PRIVATE pages)
# [measured 2026-08-10T22:30:11+00:00, bench vh2, RTX 3060 GA106, host driver 580.159.04]
# tree/binary stamp: 3437903bb422566f28d2228ba5c4b8e15c5746f0

ok    R2 version         = "580.159.04"
ok    R4 hClient         = 0xc1d01139
ok    R6 hSubdevice      = 0xcafe0002
REV_UNDER_TEST=3437903bb422566f28d2228ba5c4b8e15c5746f0
==    R30 fb-cpu-view    = gpu 0, euid 0, join Private, FIXED at 0x00007f0000000000  [NEGATIVE CONTROL: the guest-side view is PRIVATE ANONYMOUS, so it is NOT the same pages — BOTH directions must MISMATCH, and that is the PASS]
★     R30 premise        = the vidmem object `alloc_vidmem` mints IS CPU-MAPPABLE: NV_ESC_RM_MAP_MEMORY succeeded and 16384 words round-tripped through that mapping. ⊘ Through the SAME mapping, so this says the view takes stores and loads — NOT that the card holds them.
ok    R30 neg control    = export_backing(HostDeviceMemory) on that SAME live object → NotExportableAsMemory, BY NAME. ⇒ the CPU view exists and CANNOT cross to the VMM as memory, which is the whole of why `w228`'s objects have no guest-reachable view.
      R30 guest→host    = DISAGREE at word 0 (got 0x00000000, want 0xa19a5a5b) of 16384 compared
      R30 host→guest    = DISAGREE at word 0 (got 0xa19a5a5b, want 0x043ffffe) of 16384 compared
ok    R30 CONTROL FIRED  = with the guest-side view on PRIVATE pages the differential fails at word 0 in BOTH directions. ⇒ the comparison CAN fail, so the shared run's agreement is a measurement and not a tautology.
done — fb-cpu-view probe only
CONTROL_RC=0
