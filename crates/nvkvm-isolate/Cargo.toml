[package]
name = "nvkvm-isolate"
description = "The per-process sandbox port: `Isolate`/`IsolateFactory` traits + the abstract `RmBackend` RM-verb trait (arch doc §4.2 `nvkvm-rm` folded here for now). No syscalls, no ioctls — real impls live in adapter crates."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nvkvm-util.workspace = true
nvkvm-arch.workspace = true
nvkvm-vmm.workspace = true

[lints]
workspace = true
