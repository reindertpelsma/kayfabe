[package]
name = "nvkvm-fwd"
description = "Intent recovery → unprivileged host ops (arch doc §4.2): doorbell demux by vChid, per-Vas host materialization + backing publication, per-proc exec plane. Pushbuffer parser + Case-1/Case-2 tables port here in the fwd migration step."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nvkvm-util.workspace = true
nvkvm-arch.workspace = true
nvkvm-mmu.workspace = true
nvkvm-isolate.workspace = true
nvkvm-core.workspace = true
nvkvm-vmm.workspace = true
nvkvm-completion.workspace = true

[lints]
workspace = true
