// @generated by kayfabe-abi-gen — DO NOT EDIT BY HAND.
//
// Source: open-gpu-kernel-modules 610.43.02. Regenerate with:
//   cargo run --manifest-path crates/kayfabe-abi/gen/Cargo.toml -- <ogkm-root> \
//       crates/kayfabe-abi/src/generated
//
// Every struct below is a #[repr(C)] MIRROR of the C typedef named in its doc
// comment. `LAYOUT` is the generator's own layout computation; `RUSTC_OFFSETS`
// is rustc's, via `offset_of!`. The `const` block after each struct asserts they
// agree, so a layout bug is a BUILD failure, not a runtime surprise.

//! The one RM control command in the slice: `NV0080_CTRL_CMD_DMA_SET_PAGE_DIRECTORY`.
//!
//! This is where a VAS's page-directory base is declared, i.e. where the data-plane
//! identity (`Pdb`) is born — `RmEvent::SetPageDir`. It arrives inside an
//! `NVOS54_PARAMETERS` as the control payload.
//!
//! ★ Version caveat, stated because it is real: the prefix through `hVASpace` is
//! confirmed by three independent oracles (ogkm 610.43.02; the C emulator's snoop
//! offsets at `src/qemu/nvkvm_gpu_emul.c:2528-2536`, which read `physAddress@+0`,
//! `numEntries@+8`, `flags@+12`, `hVASpace@+16`; and NVIDIA's own field order). The
//! tail (`chId`, `subDeviceId`, `pasid`) is confirmed only by ogkm 610.43.02 — no
//! other oracle models this struct at all. `pasid` in particular looks like a recent
//! addition, in the same family as the `NV_VASPACE_ALLOCATION_PARAMETERS` `+Pasid`
//! growth the C artifact records at 580. If a 575 guest sends a shorter payload,
//! `decode` refuses **loudly** rather than reading past it, which is the correct
//! failure.

use crate::wire::{AbiError, Field, StructLayout, u32_at, u64_at};

/// `NV0080_CTRL_CMD_DMA_SET_PAGE_DIRECTORY` — the control command whose
/// payload is [`Nv0080CtrlDmaSetPageDirectoryParams`].
///
/// ogkm `src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h`.
pub const NV0080_CTRL_CMD_DMA_SET_PAGE_DIRECTORY: u32 = 0x801813;

/// `NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS` — ogkm `src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:802`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nv0080CtrlDmaSetPageDirectoryParams {
    /// `NvU64 physAddress` @ +0 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:803).
    pub phys_address: u64,
    /// `NvU32 numEntries` @ +8 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:804).
    pub num_entries: u32,
    /// `NvU32 flags` @ +12 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:805).
    pub flags: u32,
    /// `NvHandle hVASpace` @ +16 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:806).
    pub h_va_space: u32,
    /// `NvU32 chId` @ +20 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:807).
    pub ch_id: u32,
    /// `NvU32 subDeviceId` @ +24 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:808).
    pub sub_device_id: u32,
    /// `NvU32 pasid` @ +28 (src/common/sdk/nvidia/inc/ctrl/ctrl0080/ctrl0080dma.h:809).
    pub pasid: u32,
}

impl Nv0080CtrlDmaSetPageDirectoryParams {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS";
    /// `sizeof(NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 32;
    /// `alignof(NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS",
        size: 32,
        align: 8,
        fields: &[
            Field {
                c_name: "physAddress",
                rust_name: "phys_address",
                offset: 0,
                width: 8,
            },
            Field {
                c_name: "numEntries",
                rust_name: "num_entries",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "hVASpace",
                rust_name: "h_va_space",
                offset: 16,
                width: 4,
            },
            Field {
                c_name: "chId",
                rust_name: "ch_id",
                offset: 20,
                width: 4,
            },
            Field {
                c_name: "subDeviceId",
                rust_name: "sub_device_id",
                offset: 24,
                width: 4,
            },
            Field {
                c_name: "pasid",
                rust_name: "pasid",
                offset: 28,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        (
            "phys_address",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, phys_address),
        ),
        (
            "num_entries",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, num_entries),
        ),
        (
            "flags",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, flags),
        ),
        (
            "h_va_space",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, h_va_space),
        ),
        (
            "ch_id",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, ch_id),
        ),
        (
            "sub_device_id",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, sub_device_id),
        ),
        (
            "pasid",
            core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, pasid),
        ),
    ];

    /// Decode from a little-endian byte image of `NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            phys_address: u64_at(bytes, 0)?,
            num_entries: u32_at(bytes, 8)?,
            flags: u32_at(bytes, 12)?,
            h_va_space: u32_at(bytes, 16)?,
            ch_id: u32_at(bytes, 20)?,
            sub_device_id: u32_at(bytes, 24)?,
            pasid: u32_at(bytes, 28)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.phys_address.to_le_bytes();
            bytes
                .get_mut(0..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.num_entries.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_va_space.to_le_bytes();
            bytes
                .get_mut(16..20)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.ch_id.to_le_bytes();
            bytes
                .get_mut(20..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.sub_device_id.to_le_bytes();
            bytes
                .get_mut(24..28)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.pasid.to_le_bytes();
            bytes
                .get_mut(28..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(
        core::mem::size_of::<Nv0080CtrlDmaSetPageDirectoryParams>()
            == Nv0080CtrlDmaSetPageDirectoryParams::SIZE
    );
    assert!(
        core::mem::align_of::<Nv0080CtrlDmaSetPageDirectoryParams>()
            == Nv0080CtrlDmaSetPageDirectoryParams::ALIGN
    );
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, phys_address) == 0);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, num_entries) == 8);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, flags) == 12);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, h_va_space) == 16);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, ch_id) == 20);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, sub_device_id) == 24);
    assert!(core::mem::offset_of!(Nv0080CtrlDmaSetPageDirectoryParams, pasid) == 28);
};

/// Every struct this module generates, in declaration order — the enumerated-vs-
/// exercised coverage surface (`mode2_abi_agnostic_layer.md` §2.3, rule 2).
pub const STRUCTS: &[&StructLayout] = &[&Nv0080CtrlDmaSetPageDirectoryParams::LAYOUT];

/// The generator-computed offsets paired with rustc's own, per struct. The
/// crate's tests walk this so the agreement is also a RUNTIME assertion the
/// mutation gate can see.
pub const RUSTC_OFFSETS: &[(&str, &[(&str, usize)])] = &[(
    "NV0080_CTRL_DMA_SET_PAGE_DIRECTORY_PARAMS",
    Nv0080CtrlDmaSetPageDirectoryParams::RUSTC_OFFSETS,
)];
