// @generated by kayfabe-abi-gen — DO NOT EDIT BY HAND.
//
// Source: open-gpu-kernel-modules 610.43.02. Regenerate with:
//   cargo run --manifest-path crates/kayfabe-abi/gen/Cargo.toml -- <ogkm-root> \
//       crates/kayfabe-abi/src/generated
//
// Every struct below is a #[repr(C)] MIRROR of the C typedef named in its doc
// comment. `LAYOUT` is the generator's own layout computation; `RUSTC_OFFSETS`
// is rustc's, via `offset_of!`. The `const` block after each struct asserts they
// agree, so a layout bug is a BUILD failure, not a runtime surprise.

//! The frontend RM ioctl parameter structs — the seven verbs the core's `RmEvent`
//! enum is made of.
//!
//! | C typedef | escape | `RmEvent` |
//! |---|---|---|
//! | `NVOS00_PARAMETERS` | `NV_ESC_RM_FREE` | `Free` |
//! | `NVOS21_PARAMETERS` | `NV_ESC_RM_ALLOC` (v1) | `Alloc` |
//! | `NVOS64_PARAMETERS` | `NV_ESC_RM_ALLOC` (v2) | `Alloc` |
//! | `NVOS54_PARAMETERS` | `NV_ESC_RM_CONTROL` | carries `SetPageDir` |
//! | `NVOS55_PARAMETERS` | `NV_ESC_RM_DUP_OBJECT` | `Dup` |
//! | `NVOS46_PARAMETERS` | `NV_ESC_RM_MAP_MEMORY_DMA` | `MapMemoryDma` |
//! | `NVOS47_PARAMETERS` | `NV_ESC_RM_UNMAP_MEMORY_DMA` | `Unmap` |
//!
//! ★ `NVOS46_PARAMETERS` as generated here is the **580.65.06-and-later** layout
//! (64 bytes, with `flags2` + `kindOverride`). The pre-580.65.06 layout is 56 bytes
//! and is NOT in this file, because the vendored ogkm tree is a single snapshot
//! (610.43.02) — see `crate::transcribed`.

use crate::wire::{AbiError, Field, StructLayout, u32_at, u64_at};

/// `NV_ESC_RM_FREE` — ioctl NR carrying [`Nvos00Parameters`].
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_FREE: u32 = 0x29;

/// `NV_ESC_RM_CONTROL` — ioctl NR carrying [`Nvos54Parameters`].
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_CONTROL: u32 = 0x2a;

/// `NV_ESC_RM_ALLOC` — ioctl NR carrying [`Nvos21Parameters`] or
/// [`Nvos64Parameters`], discriminated by the ioctl's own size field.
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_ALLOC: u32 = 0x2b;

/// `NV_ESC_RM_DUP_OBJECT` — ioctl NR carrying [`Nvos55Parameters`].
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_DUP_OBJECT: u32 = 0x34;

/// `NV_ESC_RM_MAP_MEMORY_DMA` — ioctl NR carrying [`Nvos46Parameters`].
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_MAP_MEMORY_DMA: u32 = 0x57;

/// `NV_ESC_RM_UNMAP_MEMORY_DMA` — ioctl NR carrying [`Nvos47Parameters`].
///
/// ogkm `src/nvidia/arch/nvalloc/unix/include/nv_escape.h`.
pub const NV_ESC_RM_UNMAP_MEMORY_DMA: u32 = 0x58;

/// `NVOS00_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:162`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos00Parameters {
    /// `NvHandle hRoot` @ +0 (src/common/sdk/nvidia/inc/nvos.h:164).
    pub h_root: u32,
    /// `NvHandle hObjectParent` @ +4 (src/common/sdk/nvidia/inc/nvos.h:165).
    pub h_object_parent: u32,
    /// `NvHandle hObjectOld` @ +8 (src/common/sdk/nvidia/inc/nvos.h:166).
    pub h_object_old: u32,
    /// `NvV32 status` @ +12 (src/common/sdk/nvidia/inc/nvos.h:167).
    pub status: u32,
}

impl Nvos00Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS00_PARAMETERS";
    /// `sizeof(NVOS00_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 16;
    /// `alignof(NVOS00_PARAMETERS)`.
    pub const ALIGN: usize = 4;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS00_PARAMETERS",
        size: 16,
        align: 4,
        fields: &[
            Field {
                c_name: "hRoot",
                rust_name: "h_root",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hObjectParent",
                rust_name: "h_object_parent",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hObjectOld",
                rust_name: "h_object_old",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 12,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        ("h_root", core::mem::offset_of!(Nvos00Parameters, h_root)),
        (
            "h_object_parent",
            core::mem::offset_of!(Nvos00Parameters, h_object_parent),
        ),
        (
            "h_object_old",
            core::mem::offset_of!(Nvos00Parameters, h_object_old),
        ),
        ("status", core::mem::offset_of!(Nvos00Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS00_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_root: u32_at(bytes, 0)?,
            h_object_parent: u32_at(bytes, 4)?,
            h_object_old: u32_at(bytes, 8)?,
            status: u32_at(bytes, 12)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_root.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_parent.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_old.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos00Parameters>() == Nvos00Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos00Parameters>() == Nvos00Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos00Parameters, h_root) == 0);
    assert!(core::mem::offset_of!(Nvos00Parameters, h_object_parent) == 4);
    assert!(core::mem::offset_of!(Nvos00Parameters, h_object_old) == 8);
    assert!(core::mem::offset_of!(Nvos00Parameters, status) == 12);
};

/// `NVOS21_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:464`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos21Parameters {
    /// `NvHandle hRoot` @ +0 (src/common/sdk/nvidia/inc/nvos.h:466).
    pub h_root: u32,
    /// `NvHandle hObjectParent` @ +4 (src/common/sdk/nvidia/inc/nvos.h:467).
    pub h_object_parent: u32,
    /// `NvHandle hObjectNew` @ +8 (src/common/sdk/nvidia/inc/nvos.h:468).
    pub h_object_new: u32,
    /// `NvV32 hClass` @ +12 (src/common/sdk/nvidia/inc/nvos.h:469).
    pub h_class: u32,
    /// `NvP64 pAllocParms` @ +16 (src/common/sdk/nvidia/inc/nvos.h:470).
    pub p_alloc_parms: u64,
    /// `NvU32 paramsSize` @ +24 (src/common/sdk/nvidia/inc/nvos.h:471).
    pub params_size: u32,
    /// `NvV32 status` @ +28 (src/common/sdk/nvidia/inc/nvos.h:472).
    pub status: u32,
}

impl Nvos21Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS21_PARAMETERS";
    /// `sizeof(NVOS21_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 32;
    /// `alignof(NVOS21_PARAMETERS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS21_PARAMETERS",
        size: 32,
        align: 8,
        fields: &[
            Field {
                c_name: "hRoot",
                rust_name: "h_root",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hObjectParent",
                rust_name: "h_object_parent",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hObjectNew",
                rust_name: "h_object_new",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "hClass",
                rust_name: "h_class",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "pAllocParms",
                rust_name: "p_alloc_parms",
                offset: 16,
                width: 8,
            },
            Field {
                c_name: "paramsSize",
                rust_name: "params_size",
                offset: 24,
                width: 4,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 28,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        ("h_root", core::mem::offset_of!(Nvos21Parameters, h_root)),
        (
            "h_object_parent",
            core::mem::offset_of!(Nvos21Parameters, h_object_parent),
        ),
        (
            "h_object_new",
            core::mem::offset_of!(Nvos21Parameters, h_object_new),
        ),
        ("h_class", core::mem::offset_of!(Nvos21Parameters, h_class)),
        (
            "p_alloc_parms",
            core::mem::offset_of!(Nvos21Parameters, p_alloc_parms),
        ),
        (
            "params_size",
            core::mem::offset_of!(Nvos21Parameters, params_size),
        ),
        ("status", core::mem::offset_of!(Nvos21Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS21_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_root: u32_at(bytes, 0)?,
            h_object_parent: u32_at(bytes, 4)?,
            h_object_new: u32_at(bytes, 8)?,
            h_class: u32_at(bytes, 12)?,
            p_alloc_parms: u64_at(bytes, 16)?,
            params_size: u32_at(bytes, 24)?,
            status: u32_at(bytes, 28)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_root.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_parent.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_new.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_class.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.p_alloc_parms.to_le_bytes();
            bytes
                .get_mut(16..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.params_size.to_le_bytes();
            bytes
                .get_mut(24..28)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(28..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos21Parameters>() == Nvos21Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos21Parameters>() == Nvos21Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos21Parameters, h_root) == 0);
    assert!(core::mem::offset_of!(Nvos21Parameters, h_object_parent) == 4);
    assert!(core::mem::offset_of!(Nvos21Parameters, h_object_new) == 8);
    assert!(core::mem::offset_of!(Nvos21Parameters, h_class) == 12);
    assert!(core::mem::offset_of!(Nvos21Parameters, p_alloc_parms) == 16);
    assert!(core::mem::offset_of!(Nvos21Parameters, params_size) == 24);
    assert!(core::mem::offset_of!(Nvos21Parameters, status) == 28);
};

/// `NVOS46_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:2168`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos46Parameters {
    /// `NvHandle hClient` @ +0 (src/common/sdk/nvidia/inc/nvos.h:2170).
    pub h_client: u32,
    /// `NvHandle hDevice` @ +4 (src/common/sdk/nvidia/inc/nvos.h:2171).
    pub h_device: u32,
    /// `NvHandle hDma` @ +8 (src/common/sdk/nvidia/inc/nvos.h:2172).
    pub h_dma: u32,
    /// `NvHandle hMemory` @ +12 (src/common/sdk/nvidia/inc/nvos.h:2173).
    pub h_memory: u32,
    /// `NvU64 offset` @ +16 (src/common/sdk/nvidia/inc/nvos.h:2174).
    pub offset: u64,
    /// `NvU64 length` @ +24 (src/common/sdk/nvidia/inc/nvos.h:2175).
    pub length: u64,
    /// `NvV32 flags` @ +32 (src/common/sdk/nvidia/inc/nvos.h:2176).
    pub flags: u32,
    /// `NvV32 flags2` @ +36 (src/common/sdk/nvidia/inc/nvos.h:2177).
    pub flags2: u32,
    /// `NvV32 kindOverride` @ +40 (src/common/sdk/nvidia/inc/nvos.h:2178).
    pub kind_override: u32,
    /// `NvU64 dmaOffset` @ +48 (src/common/sdk/nvidia/inc/nvos.h:2179).
    pub dma_offset: u64,
    /// `NvV32 status` @ +56 (src/common/sdk/nvidia/inc/nvos.h:2183).
    pub status: u32,
}

impl Nvos46Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS46_PARAMETERS";
    /// `sizeof(NVOS46_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 64;
    /// `alignof(NVOS46_PARAMETERS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS46_PARAMETERS",
        size: 64,
        align: 8,
        fields: &[
            Field {
                c_name: "hClient",
                rust_name: "h_client",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hDevice",
                rust_name: "h_device",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hDma",
                rust_name: "h_dma",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "hMemory",
                rust_name: "h_memory",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "offset",
                rust_name: "offset",
                offset: 16,
                width: 8,
            },
            Field {
                c_name: "length",
                rust_name: "length",
                offset: 24,
                width: 8,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 32,
                width: 4,
            },
            Field {
                c_name: "flags2",
                rust_name: "flags2",
                offset: 36,
                width: 4,
            },
            Field {
                c_name: "kindOverride",
                rust_name: "kind_override",
                offset: 40,
                width: 4,
            },
            Field {
                c_name: "dmaOffset",
                rust_name: "dma_offset",
                offset: 48,
                width: 8,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 56,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        (
            "h_client",
            core::mem::offset_of!(Nvos46Parameters, h_client),
        ),
        (
            "h_device",
            core::mem::offset_of!(Nvos46Parameters, h_device),
        ),
        ("h_dma", core::mem::offset_of!(Nvos46Parameters, h_dma)),
        (
            "h_memory",
            core::mem::offset_of!(Nvos46Parameters, h_memory),
        ),
        ("offset", core::mem::offset_of!(Nvos46Parameters, offset)),
        ("length", core::mem::offset_of!(Nvos46Parameters, length)),
        ("flags", core::mem::offset_of!(Nvos46Parameters, flags)),
        ("flags2", core::mem::offset_of!(Nvos46Parameters, flags2)),
        (
            "kind_override",
            core::mem::offset_of!(Nvos46Parameters, kind_override),
        ),
        (
            "dma_offset",
            core::mem::offset_of!(Nvos46Parameters, dma_offset),
        ),
        ("status", core::mem::offset_of!(Nvos46Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS46_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_client: u32_at(bytes, 0)?,
            h_device: u32_at(bytes, 4)?,
            h_dma: u32_at(bytes, 8)?,
            h_memory: u32_at(bytes, 12)?,
            offset: u64_at(bytes, 16)?,
            length: u64_at(bytes, 24)?,
            flags: u32_at(bytes, 32)?,
            flags2: u32_at(bytes, 36)?,
            kind_override: u32_at(bytes, 40)?,
            dma_offset: u64_at(bytes, 48)?,
            status: u32_at(bytes, 56)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_client.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_device.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_dma.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_memory.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.offset.to_le_bytes();
            bytes
                .get_mut(16..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.length.to_le_bytes();
            bytes
                .get_mut(24..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(32..36)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags2.to_le_bytes();
            bytes
                .get_mut(36..40)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.kind_override.to_le_bytes();
            bytes
                .get_mut(40..44)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.dma_offset.to_le_bytes();
            bytes
                .get_mut(48..56)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(56..60)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos46Parameters>() == Nvos46Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos46Parameters>() == Nvos46Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos46Parameters, h_client) == 0);
    assert!(core::mem::offset_of!(Nvos46Parameters, h_device) == 4);
    assert!(core::mem::offset_of!(Nvos46Parameters, h_dma) == 8);
    assert!(core::mem::offset_of!(Nvos46Parameters, h_memory) == 12);
    assert!(core::mem::offset_of!(Nvos46Parameters, offset) == 16);
    assert!(core::mem::offset_of!(Nvos46Parameters, length) == 24);
    assert!(core::mem::offset_of!(Nvos46Parameters, flags) == 32);
    assert!(core::mem::offset_of!(Nvos46Parameters, flags2) == 36);
    assert!(core::mem::offset_of!(Nvos46Parameters, kind_override) == 40);
    assert!(core::mem::offset_of!(Nvos46Parameters, dma_offset) == 48);
    assert!(core::mem::offset_of!(Nvos46Parameters, status) == 56);
};

/// `NVOS47_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:2196`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos47Parameters {
    /// `NvHandle hClient` @ +0 (src/common/sdk/nvidia/inc/nvos.h:2198).
    pub h_client: u32,
    /// `NvHandle hDevice` @ +4 (src/common/sdk/nvidia/inc/nvos.h:2199).
    pub h_device: u32,
    /// `NvHandle hDma` @ +8 (src/common/sdk/nvidia/inc/nvos.h:2200).
    pub h_dma: u32,
    /// `NvHandle hMemory` @ +12 (src/common/sdk/nvidia/inc/nvos.h:2201).
    pub h_memory: u32,
    /// `NvV32 flags` @ +16 (src/common/sdk/nvidia/inc/nvos.h:2202).
    pub flags: u32,
    /// `NvU64 dmaOffset` @ +24 (src/common/sdk/nvidia/inc/nvos.h:2203).
    pub dma_offset: u64,
    /// `NvU64 size` @ +32 (src/common/sdk/nvidia/inc/nvos.h:2204).
    pub size: u64,
    /// `NvV32 status` @ +40 (src/common/sdk/nvidia/inc/nvos.h:2205).
    pub status: u32,
}

impl Nvos47Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS47_PARAMETERS";
    /// `sizeof(NVOS47_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 48;
    /// `alignof(NVOS47_PARAMETERS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS47_PARAMETERS",
        size: 48,
        align: 8,
        fields: &[
            Field {
                c_name: "hClient",
                rust_name: "h_client",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hDevice",
                rust_name: "h_device",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hDma",
                rust_name: "h_dma",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "hMemory",
                rust_name: "h_memory",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 16,
                width: 4,
            },
            Field {
                c_name: "dmaOffset",
                rust_name: "dma_offset",
                offset: 24,
                width: 8,
            },
            Field {
                c_name: "size",
                rust_name: "size",
                offset: 32,
                width: 8,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 40,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        (
            "h_client",
            core::mem::offset_of!(Nvos47Parameters, h_client),
        ),
        (
            "h_device",
            core::mem::offset_of!(Nvos47Parameters, h_device),
        ),
        ("h_dma", core::mem::offset_of!(Nvos47Parameters, h_dma)),
        (
            "h_memory",
            core::mem::offset_of!(Nvos47Parameters, h_memory),
        ),
        ("flags", core::mem::offset_of!(Nvos47Parameters, flags)),
        (
            "dma_offset",
            core::mem::offset_of!(Nvos47Parameters, dma_offset),
        ),
        ("size", core::mem::offset_of!(Nvos47Parameters, size)),
        ("status", core::mem::offset_of!(Nvos47Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS47_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_client: u32_at(bytes, 0)?,
            h_device: u32_at(bytes, 4)?,
            h_dma: u32_at(bytes, 8)?,
            h_memory: u32_at(bytes, 12)?,
            flags: u32_at(bytes, 16)?,
            dma_offset: u64_at(bytes, 24)?,
            size: u64_at(bytes, 32)?,
            status: u32_at(bytes, 40)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_client.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_device.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_dma.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_memory.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(16..20)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.dma_offset.to_le_bytes();
            bytes
                .get_mut(24..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.size.to_le_bytes();
            bytes
                .get_mut(32..40)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(40..44)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos47Parameters>() == Nvos47Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos47Parameters>() == Nvos47Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos47Parameters, h_client) == 0);
    assert!(core::mem::offset_of!(Nvos47Parameters, h_device) == 4);
    assert!(core::mem::offset_of!(Nvos47Parameters, h_dma) == 8);
    assert!(core::mem::offset_of!(Nvos47Parameters, h_memory) == 12);
    assert!(core::mem::offset_of!(Nvos47Parameters, flags) == 16);
    assert!(core::mem::offset_of!(Nvos47Parameters, dma_offset) == 24);
    assert!(core::mem::offset_of!(Nvos47Parameters, size) == 32);
    assert!(core::mem::offset_of!(Nvos47Parameters, status) == 40);
};

/// `NVOS54_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:2230`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos54Parameters {
    /// `NvHandle hClient` @ +0 (src/common/sdk/nvidia/inc/nvos.h:2232).
    pub h_client: u32,
    /// `NvHandle hObject` @ +4 (src/common/sdk/nvidia/inc/nvos.h:2233).
    pub h_object: u32,
    /// `NvV32 cmd` @ +8 (src/common/sdk/nvidia/inc/nvos.h:2234).
    pub cmd: u32,
    /// `NvU32 flags` @ +12 (src/common/sdk/nvidia/inc/nvos.h:2235).
    pub flags: u32,
    /// `NvP64 params` @ +16 (src/common/sdk/nvidia/inc/nvos.h:2236).
    pub params: u64,
    /// `NvU32 paramsSize` @ +24 (src/common/sdk/nvidia/inc/nvos.h:2237).
    pub params_size: u32,
    /// `NvV32 status` @ +28 (src/common/sdk/nvidia/inc/nvos.h:2238).
    pub status: u32,
}

impl Nvos54Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS54_PARAMETERS";
    /// `sizeof(NVOS54_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 32;
    /// `alignof(NVOS54_PARAMETERS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS54_PARAMETERS",
        size: 32,
        align: 8,
        fields: &[
            Field {
                c_name: "hClient",
                rust_name: "h_client",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hObject",
                rust_name: "h_object",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "cmd",
                rust_name: "cmd",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "params",
                rust_name: "params",
                offset: 16,
                width: 8,
            },
            Field {
                c_name: "paramsSize",
                rust_name: "params_size",
                offset: 24,
                width: 4,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 28,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        (
            "h_client",
            core::mem::offset_of!(Nvos54Parameters, h_client),
        ),
        (
            "h_object",
            core::mem::offset_of!(Nvos54Parameters, h_object),
        ),
        ("cmd", core::mem::offset_of!(Nvos54Parameters, cmd)),
        ("flags", core::mem::offset_of!(Nvos54Parameters, flags)),
        ("params", core::mem::offset_of!(Nvos54Parameters, params)),
        (
            "params_size",
            core::mem::offset_of!(Nvos54Parameters, params_size),
        ),
        ("status", core::mem::offset_of!(Nvos54Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS54_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_client: u32_at(bytes, 0)?,
            h_object: u32_at(bytes, 4)?,
            cmd: u32_at(bytes, 8)?,
            flags: u32_at(bytes, 12)?,
            params: u64_at(bytes, 16)?,
            params_size: u32_at(bytes, 24)?,
            status: u32_at(bytes, 28)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_client.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.cmd.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.params.to_le_bytes();
            bytes
                .get_mut(16..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.params_size.to_le_bytes();
            bytes
                .get_mut(24..28)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(28..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos54Parameters>() == Nvos54Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos54Parameters>() == Nvos54Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos54Parameters, h_client) == 0);
    assert!(core::mem::offset_of!(Nvos54Parameters, h_object) == 4);
    assert!(core::mem::offset_of!(Nvos54Parameters, cmd) == 8);
    assert!(core::mem::offset_of!(Nvos54Parameters, flags) == 12);
    assert!(core::mem::offset_of!(Nvos54Parameters, params) == 16);
    assert!(core::mem::offset_of!(Nvos54Parameters, params_size) == 24);
    assert!(core::mem::offset_of!(Nvos54Parameters, status) == 28);
};

/// `NVOS55_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:2265`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos55Parameters {
    /// `NvHandle hClient` @ +0 (src/common/sdk/nvidia/inc/nvos.h:2267).
    pub h_client: u32,
    /// `NvHandle hParent` @ +4 (src/common/sdk/nvidia/inc/nvos.h:2268).
    pub h_parent: u32,
    /// `NvHandle hObject` @ +8 (src/common/sdk/nvidia/inc/nvos.h:2269).
    pub h_object: u32,
    /// `NvHandle hClientSrc` @ +12 (src/common/sdk/nvidia/inc/nvos.h:2270).
    pub h_client_src: u32,
    /// `NvHandle hObjectSrc` @ +16 (src/common/sdk/nvidia/inc/nvos.h:2271).
    pub h_object_src: u32,
    /// `NvU32 flags` @ +20 (src/common/sdk/nvidia/inc/nvos.h:2272).
    pub flags: u32,
    /// `NvU32 status` @ +24 (src/common/sdk/nvidia/inc/nvos.h:2273).
    pub status: u32,
}

impl Nvos55Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS55_PARAMETERS";
    /// `sizeof(NVOS55_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 28;
    /// `alignof(NVOS55_PARAMETERS)`.
    pub const ALIGN: usize = 4;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS55_PARAMETERS",
        size: 28,
        align: 4,
        fields: &[
            Field {
                c_name: "hClient",
                rust_name: "h_client",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hParent",
                rust_name: "h_parent",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hObject",
                rust_name: "h_object",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "hClientSrc",
                rust_name: "h_client_src",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "hObjectSrc",
                rust_name: "h_object_src",
                offset: 16,
                width: 4,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 20,
                width: 4,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 24,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        (
            "h_client",
            core::mem::offset_of!(Nvos55Parameters, h_client),
        ),
        (
            "h_parent",
            core::mem::offset_of!(Nvos55Parameters, h_parent),
        ),
        (
            "h_object",
            core::mem::offset_of!(Nvos55Parameters, h_object),
        ),
        (
            "h_client_src",
            core::mem::offset_of!(Nvos55Parameters, h_client_src),
        ),
        (
            "h_object_src",
            core::mem::offset_of!(Nvos55Parameters, h_object_src),
        ),
        ("flags", core::mem::offset_of!(Nvos55Parameters, flags)),
        ("status", core::mem::offset_of!(Nvos55Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS55_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_client: u32_at(bytes, 0)?,
            h_parent: u32_at(bytes, 4)?,
            h_object: u32_at(bytes, 8)?,
            h_client_src: u32_at(bytes, 12)?,
            h_object_src: u32_at(bytes, 16)?,
            flags: u32_at(bytes, 20)?,
            status: u32_at(bytes, 24)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_client.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_parent.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_client_src.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_src.to_le_bytes();
            bytes
                .get_mut(16..20)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(20..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(24..28)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos55Parameters>() == Nvos55Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos55Parameters>() == Nvos55Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos55Parameters, h_client) == 0);
    assert!(core::mem::offset_of!(Nvos55Parameters, h_parent) == 4);
    assert!(core::mem::offset_of!(Nvos55Parameters, h_object) == 8);
    assert!(core::mem::offset_of!(Nvos55Parameters, h_client_src) == 12);
    assert!(core::mem::offset_of!(Nvos55Parameters, h_object_src) == 16);
    assert!(core::mem::offset_of!(Nvos55Parameters, flags) == 20);
    assert!(core::mem::offset_of!(Nvos55Parameters, status) == 24);
};

/// `NVOS64_PARAMETERS` — ogkm `src/common/sdk/nvidia/inc/nvos.h:479`.
#[repr(C)]
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct Nvos64Parameters {
    /// `NvHandle hRoot` @ +0 (src/common/sdk/nvidia/inc/nvos.h:481).
    pub h_root: u32,
    /// `NvHandle hObjectParent` @ +4 (src/common/sdk/nvidia/inc/nvos.h:482).
    pub h_object_parent: u32,
    /// `NvHandle hObjectNew` @ +8 (src/common/sdk/nvidia/inc/nvos.h:483).
    pub h_object_new: u32,
    /// `NvV32 hClass` @ +12 (src/common/sdk/nvidia/inc/nvos.h:484).
    pub h_class: u32,
    /// `NvP64 pAllocParms` @ +16 (src/common/sdk/nvidia/inc/nvos.h:485).
    pub p_alloc_parms: u64,
    /// `NvP64 pRightsRequested` @ +24 (src/common/sdk/nvidia/inc/nvos.h:486).
    pub p_rights_requested: u64,
    /// `NvU32 paramsSize` @ +32 (src/common/sdk/nvidia/inc/nvos.h:487).
    pub params_size: u32,
    /// `NvU32 flags` @ +36 (src/common/sdk/nvidia/inc/nvos.h:488).
    pub flags: u32,
    /// `NvV32 status` @ +40 (src/common/sdk/nvidia/inc/nvos.h:489).
    pub status: u32,
}

impl Nvos64Parameters {
    /// The C typedef name.
    pub const C_NAME: &'static str = "NVOS64_PARAMETERS";
    /// `sizeof(NVOS64_PARAMETERS)`, generator-computed and asserted against rustc below.
    pub const SIZE: usize = 48;
    /// `alignof(NVOS64_PARAMETERS)`.
    pub const ALIGN: usize = 8;
    /// The generator's field-by-field layout.
    pub const LAYOUT: StructLayout = StructLayout {
        c_name: "NVOS64_PARAMETERS",
        size: 48,
        align: 8,
        fields: &[
            Field {
                c_name: "hRoot",
                rust_name: "h_root",
                offset: 0,
                width: 4,
            },
            Field {
                c_name: "hObjectParent",
                rust_name: "h_object_parent",
                offset: 4,
                width: 4,
            },
            Field {
                c_name: "hObjectNew",
                rust_name: "h_object_new",
                offset: 8,
                width: 4,
            },
            Field {
                c_name: "hClass",
                rust_name: "h_class",
                offset: 12,
                width: 4,
            },
            Field {
                c_name: "pAllocParms",
                rust_name: "p_alloc_parms",
                offset: 16,
                width: 8,
            },
            Field {
                c_name: "pRightsRequested",
                rust_name: "p_rights_requested",
                offset: 24,
                width: 8,
            },
            Field {
                c_name: "paramsSize",
                rust_name: "params_size",
                offset: 32,
                width: 4,
            },
            Field {
                c_name: "flags",
                rust_name: "flags",
                offset: 36,
                width: 4,
            },
            Field {
                c_name: "status",
                rust_name: "status",
                offset: 40,
                width: 4,
            },
        ],
    };

    /// rustc's own offsets for the same fields, in the same order.
    pub const RUSTC_OFFSETS: &'static [(&'static str, usize)] = &[
        ("h_root", core::mem::offset_of!(Nvos64Parameters, h_root)),
        (
            "h_object_parent",
            core::mem::offset_of!(Nvos64Parameters, h_object_parent),
        ),
        (
            "h_object_new",
            core::mem::offset_of!(Nvos64Parameters, h_object_new),
        ),
        ("h_class", core::mem::offset_of!(Nvos64Parameters, h_class)),
        (
            "p_alloc_parms",
            core::mem::offset_of!(Nvos64Parameters, p_alloc_parms),
        ),
        (
            "p_rights_requested",
            core::mem::offset_of!(Nvos64Parameters, p_rights_requested),
        ),
        (
            "params_size",
            core::mem::offset_of!(Nvos64Parameters, params_size),
        ),
        ("flags", core::mem::offset_of!(Nvos64Parameters, flags)),
        ("status", core::mem::offset_of!(Nvos64Parameters, status)),
    ];

    /// Decode from a little-endian byte image of `NVOS64_PARAMETERS`.
    ///
    /// Accepts a buffer of at least [`Self::SIZE`] bytes and ignores anything
    /// past it (a longer buffer is a legitimate newer-ABI image, or a flexible
    /// array tail). A SHORTER buffer is refused loudly — silently zero-extending
    /// a truncated struct is the `abi_struct_truncation` bug class verbatim.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn decode(bytes: &[u8]) -> Result<Self, AbiError> {
        if bytes.len() < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: bytes.len(),
            });
        }
        Ok(Self {
            h_root: u32_at(bytes, 0)?,
            h_object_parent: u32_at(bytes, 4)?,
            h_object_new: u32_at(bytes, 8)?,
            h_class: u32_at(bytes, 12)?,
            p_alloc_parms: u64_at(bytes, 16)?,
            p_rights_requested: u64_at(bytes, 24)?,
            params_size: u32_at(bytes, 32)?,
            flags: u32_at(bytes, 36)?,
            status: u32_at(bytes, 40)?,
        })
    }

    /// Write this value back over a little-endian byte image, in place.
    ///
    /// Writes **only** the declared fields; padding bytes and any trailing
    /// payload are left exactly as found. That is deliberate: the C-era
    /// `writeback_bug_pattern` was a sanitizer that rewrote a whole struct and
    /// so handed CUDA its own scratch state back. A writer that cannot touch a
    /// byte it does not name cannot reproduce it.
    ///
    /// # Errors
    ///
    /// [`AbiError::Truncated`] if `bytes.len() < Self::SIZE`.
    pub fn encode_into(&self, bytes: &mut [u8]) -> Result<(), AbiError> {
        let len = bytes.len();
        if len < Self::SIZE {
            return Err(AbiError::Truncated {
                c_name: Self::C_NAME,
                need: Self::SIZE,
                got: len,
            });
        }
        {
            let src = self.h_root.to_le_bytes();
            bytes
                .get_mut(0..4)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_parent.to_le_bytes();
            bytes
                .get_mut(4..8)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_object_new.to_le_bytes();
            bytes
                .get_mut(8..12)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.h_class.to_le_bytes();
            bytes
                .get_mut(12..16)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.p_alloc_parms.to_le_bytes();
            bytes
                .get_mut(16..24)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.p_rights_requested.to_le_bytes();
            bytes
                .get_mut(24..32)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.params_size.to_le_bytes();
            bytes
                .get_mut(32..36)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.flags.to_le_bytes();
            bytes
                .get_mut(36..40)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        {
            let src = self.status.to_le_bytes();
            bytes
                .get_mut(40..44)
                .ok_or(AbiError::Truncated {
                    c_name: Self::C_NAME,
                    need: Self::SIZE,
                    got: len,
                })?
                .copy_from_slice(&src);
        }
        Ok(())
    }
}

// The generator's layout vs rustc's, asserted at COMPILE time.
const _: () = {
    assert!(core::mem::size_of::<Nvos64Parameters>() == Nvos64Parameters::SIZE);
    assert!(core::mem::align_of::<Nvos64Parameters>() == Nvos64Parameters::ALIGN);
    assert!(core::mem::offset_of!(Nvos64Parameters, h_root) == 0);
    assert!(core::mem::offset_of!(Nvos64Parameters, h_object_parent) == 4);
    assert!(core::mem::offset_of!(Nvos64Parameters, h_object_new) == 8);
    assert!(core::mem::offset_of!(Nvos64Parameters, h_class) == 12);
    assert!(core::mem::offset_of!(Nvos64Parameters, p_alloc_parms) == 16);
    assert!(core::mem::offset_of!(Nvos64Parameters, p_rights_requested) == 24);
    assert!(core::mem::offset_of!(Nvos64Parameters, params_size) == 32);
    assert!(core::mem::offset_of!(Nvos64Parameters, flags) == 36);
    assert!(core::mem::offset_of!(Nvos64Parameters, status) == 40);
};

/// Every struct this module generates, in declaration order — the enumerated-vs-
/// exercised coverage surface (`mode2_abi_agnostic_layer.md` §2.3, rule 2).
pub const STRUCTS: &[&StructLayout] = &[
    &Nvos00Parameters::LAYOUT,
    &Nvos21Parameters::LAYOUT,
    &Nvos46Parameters::LAYOUT,
    &Nvos47Parameters::LAYOUT,
    &Nvos54Parameters::LAYOUT,
    &Nvos55Parameters::LAYOUT,
    &Nvos64Parameters::LAYOUT,
];

/// The generator-computed offsets paired with rustc's own, per struct. The
/// crate's tests walk this so the agreement is also a RUNTIME assertion the
/// mutation gate can see.
pub const RUSTC_OFFSETS: &[(&str, &[(&str, usize)])] = &[
    ("NVOS00_PARAMETERS", Nvos00Parameters::RUSTC_OFFSETS),
    ("NVOS21_PARAMETERS", Nvos21Parameters::RUSTC_OFFSETS),
    ("NVOS46_PARAMETERS", Nvos46Parameters::RUSTC_OFFSETS),
    ("NVOS47_PARAMETERS", Nvos47Parameters::RUSTC_OFFSETS),
    ("NVOS54_PARAMETERS", Nvos54Parameters::RUSTC_OFFSETS),
    ("NVOS55_PARAMETERS", Nvos55Parameters::RUSTC_OFFSETS),
    ("NVOS64_PARAMETERS", Nvos64Parameters::RUSTC_OFFSETS),
];
