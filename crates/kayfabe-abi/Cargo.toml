[package]
name = "kayfabe-abi"
description = "SKELETON — Axis A: the codegen'd declarative ABI (NVOS structs, class IDs, alloc-param sizes, GSP-RPC layouts, register offsets) behind the DriverAbi dispatch trait. The ONLY crate where #[repr(C)] NVIDIA wire structs will ever live."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kayfabe-util.workspace = true
kayfabe-arch.workspace = true

[lints]
workspace = true
